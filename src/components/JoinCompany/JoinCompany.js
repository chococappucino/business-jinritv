import { Button } from 'react-bootstrap';

const JoinCompany = () => {
  return (
    <section>
      <h1>Yes, I want to join the company, Sign me up!</h1>
      <Button variant="outline-dark" className="rounded-pill">
        Join the Company
      </Button>
    </section>
  );
};

export default JoinCompany;
